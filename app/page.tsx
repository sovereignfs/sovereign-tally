import { EmptyState } from '@sovereignfs/ui';

export default function TallyIndexPage() {
  return (
    <EmptyState
      heading="No groups yet"
      description="Create a group to start tracking shared expenses."
    />
  );
}
